import 'package:flutter/material.dart';

// App colors moved from utils/colors.dart
class AppColors {
  // Background colors
  static const mobileBackgroundColor = Color(0xFF0c2840);
  static const webBackgroundColor = Color(0xFF0c2840);
  
  // UI colors
  static const mobileSearchColor = Color(0xFF262626);
  static const blueColor = Color(0xFF016DB5);
  static const primaryColor = Colors.white;
  static const secondaryColor = Colors.grey;
}
